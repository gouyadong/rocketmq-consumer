package com.vdong.rocketmq.consumer.listener;

import org.springframework.stereotype.Service;


@Service
//@RocketMQMessageListener(consumerGroup = "consumer-group" ,topic = "test-topic")
//public class RocketMqConsumerListener implements RocketMQListener<UserInfo> {
public class RocketMqConsumerListener {
   /*@Override
    public void onMessage(UserInfo userInfo) {
        //当收到消息的时候执行的业务
        Integer id = userInfo.getId();
        String userName = userInfo.getUserName();
        System.out.println("id= " + id + ", userName=" + userName);

    }*/
}
